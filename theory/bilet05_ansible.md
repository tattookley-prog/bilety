# Билет №5 — Ansible

## Что требуется по заданию
Установить и настроить Ansible на BR-SRV: инвентарь, базовый `ansible.cfg`, проверка доступности управляемых узлов через `ansible all -m ping`.

## Теория
Ansible — система управления конфигурацией и автоматизации без агента на целевых машинах.
Связь обычно идёт по SSH: управляющий узел выполняет модули на удалённых хостах.
Инвентарь (`/etc/ansible/hosts`) описывает группы и адреса машин.
Модули — атомарные действия (например, `ping`, `copy`, `service`, `package`).
`ansible all -m ping` проверяет базовую управляемость и корректность SSH-доступа.
Для удобства используют SSH-ключи, чтобы не вводить пароль на каждый хост.
Параметры по умолчанию задают в `ansible.cfg` (путь к inventory, отключение host key checking и т.д.).

Для защиты полезно объяснить не только команды, но и логику потока запроса/данных:
что является точкой входа, где хранится состояние, и каким шагом подтверждается корректность.
Хорошая практика ответа: сначала назвать роль сервиса, затем ключевой конфиг, затем проверочную команду.
Так преподавателю видно, что вы понимаете причину настройки, а не только повторяете команды.

## Ключевые файлы и пути конфигурации
- `/etc/ansible/hosts`
- `/etc/ansible/ansible.cfg`
- `~/.ssh/id_rsa` и `authorized_keys` (при ключевой аутентификации)

## Основные команды
- `ansible --version` — версия и активный конфиг.
- `ansible all --list-hosts` — какие хосты попадут в команду.
- `ansible all -m ping` — проверка доступности.
- `ansible <group> -m shell -a "..."` — разовая команда на группе.

## Как проверить результат
Используйте справочник в конце скрипта: `scripts/ticket05_ansible.sh`.

## Частые вопросы преподавателя / на что обратить внимание
- Почему Ansible называют «agentless».
- Что такое inventory-группы и как они упрощают управление.
- Почему `ping` в Ansible — это не ICMP, а модульная проверка по SSH.

## Соответствующий скрипт
`scripts/ticket05_ansible.sh`
